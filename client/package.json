{
  "name": "zync-client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "fuser -k 3000/tcp 2>/dev/null; node server.js",
    "build": "next build",
    "start": "fuser -k 3000/tcp 2>/dev/null; NODE_ENV=production node server.js",
    "lint": "next lint"
  }
}
